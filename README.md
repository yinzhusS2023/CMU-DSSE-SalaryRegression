# CMU-DSSE-SalaryRegression
Apply the machine learning approach by (feature selection) a group of job related attributes (and use a regression model) to indicate the potential salary of such combination.
